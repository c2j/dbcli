# MySQL Docker connection config
# Usage: hepta_dbcli --config tests/docker-mysql.toml cli --sql "SELECT VERSION()"

default_connection = "local"

[connections.local]
host = "127.0.0.1"
port = 3306
user = "root"
password = "testpass"
database = "testdb"
